@{
    ViewBag.Title = "Rosters";
}

<h2>Rosters</h2>

<div class="row">
    <div class="col-sm-12">
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <td>Roster Name</td>
                    <td>Enrollment</td>
                </tr>
            </thead>
            <tbody data-bind="foreach: allRosters">
                <tr>
                    <td>
                        <a data-bind="attr: {href: rosterLink}, text: name">
                        </a>
                    </td>
                    <td data-bind="text: enrollment"></td>
                </tr>
            </tbody>
        </table>
    </div>
</div>

@section Scripts
{
    <script src="~/KnockoutViewModels/RosterListViewModel.js"></script>

    <script type="text/javascript">
        $(function () {
            var viewModel = ko.mapping.fromJS(@Html.Raw(Model));
            ko.applyBindings(viewModel);
        });
    </script>


}
